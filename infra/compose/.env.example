# Copied to /opt/makitii/.env on the box by the Tofu bootstrap. Not committed with real values.
GHCR_OWNER=alibaghdadi2890
VERSION=latest
# ":80" until the DNS record exists; then the hostname, e.g. makitii.example.com, and
# `docker compose up -d` — Caddy fetches a Let's Encrypt certificate automatically.
SITE_ADDRESS=:80
